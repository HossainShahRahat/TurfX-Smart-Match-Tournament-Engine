# Admin Login Credentials (plain text, hashed on first login)
# After first login, change via DB or new env (env overrides until changed)
ADMIN_EMAIL=admin@turfx.app
ADMIN_PASSWORD=admin123

# ... other existing env vars
